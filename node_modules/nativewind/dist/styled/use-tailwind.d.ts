import { StyleProp } from "react-native";
import { StylesArray } from "../style-sheet";
import { StateBitOptions } from "../utils/selector";
export interface UseTailwindOptions<T> extends StateBitOptions {
    className: string;
    inlineStyles?: StyleProp<T>;
    additionalStyles?: StylesArray<T>;
    flatten?: boolean;
}
export declare function useTailwind<T>({ className, inlineStyles, additionalStyles, hover, focus, active, isolateGroupHover, isolateGroupFocus, isolateGroupActive, groupHover, groupFocus, groupActive, flatten, }: UseTailwindOptions<T>): StylesArray<T>;
